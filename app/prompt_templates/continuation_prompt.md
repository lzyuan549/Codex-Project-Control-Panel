PROMPT_STAGE: execution

继续上一轮开发任务，请严格按 PLAN.md 执行，并持续维护状态文档。

【项目目标 / 题目】
{{PROJECT_GOAL}}

【本轮选中的未完成项】
本轮最多只推进下面 10 个未完成项。不要故意处理后续未选中的任务，除非为了完成当前项需要必要且很小的辅助改动。
{{SELECTED_BATCH}}

【执行规则】
1. 先读取 ./PLAN.md、./HANDOFF.md、./TEST_REPORT.md。
2. 用不超过 8 行总结：目标、已完成、未完成、风险、立即下一步。
3. 不得重置、删除、改写已勾选项（`- [x]`）。
4. 从本轮选中的未完成项继续执行；如果发现当前计划粒度不足或与项目实际冲突，先补细 PLAN.md，再继续。
5. 每完成一项：
   - 先更新 PLAN.md 打勾并写完成说明 + 时间（YYYY-MM-DD HH:mm +08:00）。
   - 再更新 TEST_REPORT.md（如涉及测试/检查）。
   - 再更新 HANDOFF.md。
   - 最后回复进度。
6. 每次回复固定包含：
   - 已完成：第N项（名称）。
   - 进行中：第M项（名称）。
   - 下一项：第K项（名称）。
   - 风险/阻塞：无或具体说明。
7. 如果判断上下文即将接近上限，先更新 PLAN.md、HANDOFF.md、TEST_REPORT.md，再输出“可续接提示词”。
8. 不允许虚构执行结果；未执行就写“未执行 + 原因”。
9. 运行验证命令时禁止把长期服务留在前台；如需启动 dev server、后端服务或其他常驻进程，只能用于短时验证，验证完成后必须停止相关进程并在 TEST_REPORT.md 记录命令与结果。

【通用执行原则】
1. 不预设技术栈、框架、语言、数据库、前端形态、部署方式或目录结构。
2. 以 PLAN.md 已确认的方案、当前项目实际代码、上传约束文件和现有工程风格为准。
3. 优先复用现有模块、接口、组件、脚本、数据结构、权限/状态管理方式、命名风格和测试方式。
4. 如果 PLAN.md 与当前项目实际不一致，先做最小必要修订并说明原因，再继续执行本轮任务。
5. 如果用户上传约束文件与 PLAN.md 冲突，以用户约束和最新项目实际为优先，并在 HANDOFF.md 与 TEST_REPORT.md 中记录影响。
6. 新增依赖、技术方案、目录结构或外部服务前，必须确认它们是完成当前任务所必需，并记录选择理由与风险。
7. 测试数据、示例内容和界面文案应符合项目目标、用户语言和业务语境；不要使用无意义的 test/demo 占位内容。

【当前 PLAN.md】
```markdown
{{PLAN_MD}}
```

【当前 HANDOFF.md】
```markdown
{{HANDOFF_MD}}
```

【当前 TEST_REPORT.md】
```markdown
{{TEST_REPORT_MD}}
```

【上传约束文件】
{{CONSTRAINTS}}
